let num = 10; // Initialize a variable named num with the value 10
while (num >= 20) {  // Start a while loop that continues as long as num is greater than or equal to 20
  num = num + 1; // Increment the value of num by 1 in each iteration
  console.log(num); // Print the current value of num to the console
}

for(let i = 1; i <= 5; i++){ // Start a for loop that initializes i to 1, continues as long as i is less than or equal to 5, and increments i by 1 in each iteration
    console.log(i); // Print the current value of i to the console
}